services:
  web:
    ports:
      - "8000:8000"
    command: >
      sh -c "python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"
    volumes:


  celery_worker:
    volumes:
      - media_data:/app/media

  celery_beat:
    volumes:


  nginx:
    profiles:
      - nginx